#include<func.h>

int main(int argc,char *argv[])
{
	ARGS_CHECK(argc,2 );
	int ret;
	ret =stat buf;
	int ret;
	ret stat();
	ERROR_CHECK(ret,-1,"stat");
	printf("%ld %x %ld %d %ld %s\n",buf.st_ino,buf.st_
